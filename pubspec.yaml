name: yessine
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  dio: ^5.0.0    # Assurez-vous d'ajouter la dernière version de dio
  permission_handler: ^11.3.1
  cached_network_image: ^3.4.1






  firebase_core: ^3.6.0
  firebase_auth: ^5.3.4
  firebase_app_check: ^0.3.1+7


  cloud_firestore: ^5.4.4
  firebase_storage: ^12.3.3
  firebase_cloud_messaging_flutter: ^1.0.6

  get: ^4.6.1
  cupertino_icons: ^1.0.6
  http: ^1.2.1
  provider: ^6.0.1



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  #Get Cli yaawnek bech tesna3 module keml hather bil controller w bil view mte3o yekhdm al architecture mvc w maa getx
flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700